import { PlaceholderPage } from "@/components/placeholder-page";

export default function CampaignsPage() {
  return (
    <PlaceholderPage
      title="Campaigns"
      phase="Coming in Phase 4"
      description="WhatsApp template broadcasts to patient segments — recalls and reactivation."
    />
  );
}
